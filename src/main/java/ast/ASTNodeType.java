package ast;

public enum ASTNodeType {
    LITERAL, ID, VAR_DECL, BIN_OP, UN_OP, DEF, DTYPE, SCOPE, FUN_CALL, FUN_DEF,
    PARAM_LIST, PARAM_DECL, RET, IF_ELSE, IF, ELSE, WHILE, TYPE_ANN, BREAK, CONT, ARR_LITERAL,
    ARR_ACCESS
}
